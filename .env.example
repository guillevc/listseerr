# User and Group IDs for file permissions
# Run `id -u` and `id -g` on your host to get your values
# Default: 1000:1000 (most common on Linux)
UID=1000
GID=1000

# Application Configuration
PORT=3000
LOG_LEVEL=info
DATABASE_PATH=/app/data/listseerr.db

# Encryption key for API keys/client IDs (generate with: openssl rand -hex 32)
# IMPORTANT: Generate a unique key for each environment (dev/staging/prod)
# Never commit your actual encryption key to version control
ENCRYPTION_KEY=your_64_char_hex_key_here
