# For testing production build locally with: `bun run start
# Copy to .env and update as needed.
# All paths are relative to project root.

# Server Configuration
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=production

# Database Configuration
DATABASE_PATH=./data/listseerr.db
MIGRATIONS_FOLDER=./packages/server/migrations
# (REQUIRED) Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_64_char_hex_key_here_replace_this_in_production_env
