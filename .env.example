# ==============================================================================
# DOCKER ENVIRONMENT VARIABLES
# ==============================================================================
# This file is for docker-compose configuration.
# Copy to .env and update for your deployment.
# Uses absolute container paths.
# ==============================================================================

# User/Group IDs (for Docker file permissions)
# Find your values with: id -u (UID) and id -g (GID)
UID=1000
GID=1000

# Server Configuration
PORT=3000
LOG_LEVEL=info
NODE_ENV=production

# Database Configuration (absolute container paths)
DATABASE_PATH=/app/data/listseerr.db
MIGRATIONS_FOLDER=/app/migrations

# Security (REQUIRED)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_64_char_hex_key_here_replace_this_in_production_env
