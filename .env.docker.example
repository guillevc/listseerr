# This file is for Docker configuration.
# Copy to .env.docker and update for your deployment.
# Uses absolute container paths.

# User/Group IDs (for Docker file permissions)
# Find your values with: id -u (UID) and id -g (GID)
UID=1000
GID=1000

# Server Configuration
PORT=3000
LOG_LEVEL=info
NODE_ENV=production

# Database Configuration
DATABASE_PATH=/app/data/listseerr.docker.db
MIGRATIONS_FOLDER=/app/migrations
# (REQUIRED) Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_64_char_hex_key_here_replace_this_in_production_env
